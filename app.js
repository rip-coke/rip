#!/usr/bin/env node

import PaystackPop from '@paystack/inline-js'

var args = process.argv.slice(2);

console.log(index + ': ' + val);
const popup = new PaystackPop()
popup.resumeTransaction("np00q2xw1l")
